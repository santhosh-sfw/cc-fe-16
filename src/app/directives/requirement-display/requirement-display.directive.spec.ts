import { RequirementDisplayDirective } from './requirement-display.directive';

describe('RequirementDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new RequirementDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
