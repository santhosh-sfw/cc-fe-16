import { FieldFocusedDirective } from './field-focused.directive';

describe('FieldFocusedDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldFocusedDirective();
    expect(directive).toBeTruthy();
  });
});
