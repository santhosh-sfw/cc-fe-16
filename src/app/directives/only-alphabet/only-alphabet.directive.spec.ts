import { OnlyAlphabetDirective } from './only-alphabet.directive';

describe('OnlyAlphabetDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphabetDirective();
    expect(directive).toBeTruthy();
  });
});
