import { DatepickerLabelRaiseDirective } from './datepicker-label-raise.directive';

describe('DatepickerLabelRaiseDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerLabelRaiseDirective();
    expect(directive).toBeTruthy();
  });
});
