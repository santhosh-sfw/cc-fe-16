import { SidebarResponsiveDirective } from './sidebar-responsive.directive';

describe('SidebarResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
