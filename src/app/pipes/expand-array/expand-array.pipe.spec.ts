import { ExpandArrayPipe } from './expand-array.pipe';

describe('ExpandArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpandArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
