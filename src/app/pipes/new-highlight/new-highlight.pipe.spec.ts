import { NewHighlightPipe } from './new-highlight.pipe';

describe('NewHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new NewHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
