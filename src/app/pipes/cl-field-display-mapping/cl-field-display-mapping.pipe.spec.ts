import { ClFieldDisplayMappingPipe } from './cl-field-display-mapping.pipe';

describe('ClFieldDisplayMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new ClFieldDisplayMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
